# LogMind-Agentic-CI-Test-Failure-Triage
An LLM agent that ingests CI test logs, triages failures, and generates structured bug reports — without human intervention. When uncertain, it flags for manual review rather than hallucinating.
